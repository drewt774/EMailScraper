Script will parse a website and find the e-mail addresses using REGEX.

To run:
$ python pagescanner.py

TODO:
** Enable scanning of entire website and not just a single page
** Enable multiple top-level domains to be scanned within a single run
** Build UI shell
